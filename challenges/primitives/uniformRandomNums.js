/**
 * Given a random number generator that products a value
 * between 0 and 1 as well as two integers x and y, return a
 * generator that returns values between a and b inclusively
 * with equal probability.
 *
 * Example: x = 1, y = 10
 * Returns: a function with equal probability to
 * generate the numbers 1 through 10
 */

function uninformRandoms (randZeroToOne, x, y) {
  // TODO
}

export default uninformRandoms
